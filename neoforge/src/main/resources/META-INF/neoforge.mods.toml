modLoader="javafml"
license="${mod_license}"
loaderVersion="${neoforge_loader_version_range}"
issueTrackerURL="${mod_issues}"
displayURL="${mod_page}"
logoFile="icon.png"

[[mixins]]
config="exposure_catalog-common.mixins.json"

[[mixins]]
config="exposure_catalog-neoforge.mixins.json"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
authors="${mod_authors}"
description="${mod_description}"

[[dependencies.${mod_id}]]
modId="neoforge"
required=true
versionRange="${neoforge_version_range}"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId = "minecraft"
type = "required"
versionRange = "${minecraft_version_range}"
ordering = "NONE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId="exposure"
mandatory=true
versionRange="${required_exposure_version_range_forge}"
ordering="NONE"
side="BOTH"